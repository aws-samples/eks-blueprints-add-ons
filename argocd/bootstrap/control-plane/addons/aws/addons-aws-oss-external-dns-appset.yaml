apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons-aws-external-dns
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - merge:
        mergeKeys:
          - server
        generators:
          - clusters:
              values:
                addonChart: external-dns
                # anything not staging or prod use this version
                addonChartVersion: '1.12.2'
                addonChartRepository: https://kubernetes-sigs.github.io/external-dns
              selector:
                matchExpressions:
                - key: akuity.io/argo-cd-cluster-name
                  operator: NotIn
                  values:
                    - in-cluster
                - key: enable_aws_external_dns
                  operator: In
                  values:
                    - 'true'
          - clusters:
              selector:
                matchLabels:
                  environment: staging
              values:
                addonChartVersion: '1.12.2'
          - clusters:
              selector:
                matchLabels:
                  environment: prod
              values:
                addonChartVersion: '1.12.2'
  template:
    metadata:
      name: 'addon-{{name}}-{{values.addonChart}}'
    spec:
      project: default
      sources:
      - repoURL: 'https://github.com/csantanapr/eks-blueprints-add-ons'
        targetRevision: gitops-bridge-v2
        ref: values
      - chart: '{{values.addonChart}}'
        repoURL: '{{values.addonChartRepository}}'
        targetRevision: '{{values.addonChartVersion}}'
        helm:
          releaseName: '{{values.addonChart}}'
          ignoreMissingValueFiles: true
          valueFiles:
          - $values/argocd/charts/addons/{{values.addonChart}}/values.yaml
          - $values/argocd/environments/{{metadata.labels.environment}}/addons/{{values.addonChart}}/values.yaml
          - $values/argocd/clusters/{{name}}/addons/{{values.addonChart}}/values.yaml
          values: |
            provider: aws
            serviceAccount:
              name: {{metadata.annotations.metadata_aws_external_dns_service_account}}
              annotations:
                eks.amazonaws.com/role-arn: {{metadata.annotations.metadata_aws_external_dns_iam_role_arn}}
            domainFilters: {{metadata.annotations.metadata_aws_external_dns_domain_filters}}
      destination:
        namespace: '{{metadata.annotations.metadata_aws_external_dns_namespace}}'
        name: '{{name}}'
      syncPolicy:
        automated: {}
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true  # Big CRDs.
